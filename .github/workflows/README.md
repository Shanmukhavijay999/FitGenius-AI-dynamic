# GitHub Actions CI/CD

This directory contains GitHub Actions workflows to automate tests, building, and deployment of the frontend and backend.

## Workflows
- `ci.yml`: Runs tests, code quality checks (linting/formatting) on pull requests.
- `cd.yml`: Handles automated deployments to Vercel (frontend) and Render (backend).
