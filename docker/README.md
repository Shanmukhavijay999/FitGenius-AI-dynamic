# Docker Containers

This directory contains container configurations for local development and orchestration.

## Contents
- `Dockerfile.frontend`: Multi-stage build for Next.js app.
- `Dockerfile.backend`: FastAPI container environment with Python 3.12, OpenCV, and dependencies.
- `docker-compose.yml`: Local orchestrator for backend services, database, and local environment.
