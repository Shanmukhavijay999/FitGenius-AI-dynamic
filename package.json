{
  "name": "fitgenius-ai",
  "version": "1.0.0",
  "description": "FitGenius AI - Dynamic Size & Fit Chart Generator",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "concurrently --names \"BACKEND,FRONTEND\" \"npm start --prefix backend\" \"npm start --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
